{
  "version": 1,
  "Resources": [
    {
      "TargetService": {
        "Type": "AWS::ECS::Service",
        "Properties": {
          "TaskDefinition": "$TASK_ARN",
          "LoadBalancerInfo": {
            "ContainerName": "$APP_NAME",
            "ContainerPort": $CONTAINER_PORT
          }
        }
      }
    }
  ]
}
